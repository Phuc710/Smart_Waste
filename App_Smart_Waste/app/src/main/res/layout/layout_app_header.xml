<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/appHeaderRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/app_surface"
    android:gravity="center_vertical"
    android:minHeight="@dimen/header_height"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/header_horizontal_padding"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- 1. Optional Navigation Icon (Back button, Hamburger menu) -->
    <FrameLayout
        android:id="@+id/btnHeaderNav"
        android:layout_width="@dimen/header_icon_button_size"
        android:layout_height="@dimen/header_icon_button_size"
        android:layout_marginEnd="10dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone">

        <ImageView
            android:id="@+id/ivHeaderNavIcon"
            android:layout_width="@dimen/header_icon_size"
            android:layout_height="@dimen/header_icon_size"
            android:layout_gravity="center"
            android:contentDescription="Quay lại / Menu"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/app_text_primary" />
    </FrameLayout>

    <!-- 2. Standardized Page Title & Subtitle -->
    <LinearLayout
        android:id="@+id/llHeaderTitleContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvHeaderTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Page Title"
            android:textColor="@color/app_text_primary"
            android:textSize="@dimen/header_title_size"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvHeaderSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Subtitle"
            android:textColor="@color/app_text_secondary"
            android:textSize="@dimen/header_subtitle_size"
            android:visibility="gone" />
    </LinearLayout>

    <!-- 3. Standardized Action Buttons Container -->
    <LinearLayout
        android:id="@+id/llHeaderActionsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Text Action Button (e.g. "Đánh dấu đã đọc", "Lưu", "Xong") -->
        <FrameLayout
            android:id="@+id/btnHeaderTextAction"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvHeaderTextAction"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:gravity="center"
                android:paddingHorizontal="10dp"
                android:text="Đánh dấu đã đọc"
                android:textColor="@color/app_success"
                android:textSize="13.5sp"
                android:textStyle="bold" />
        </FrameLayout>

        <!-- Secondary Icon Button (e.g. Filter, Search) -->
        <FrameLayout
            android:id="@+id/btnHeaderActionSecondary"
            android:layout_width="@dimen/header_icon_button_size"
            android:layout_height="@dimen/header_icon_button_size"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivHeaderActionSecondaryIcon"
                android:layout_width="@dimen/header_icon_size"
                android:layout_height="@dimen/header_icon_size"
                android:layout_gravity="center"
                android:contentDescription="Hành động phụ"
                android:src="@drawable/ic_chevron_right_gray"
                app:tint="@color/app_text_primary" />
        </FrameLayout>

        <!-- Primary Icon Button (e.g. Refresh, Settings, Bell) -->
        <FrameLayout
            android:id="@+id/btnHeaderAction"
            android:layout_width="@dimen/header_icon_button_size"
            android:layout_height="@dimen/header_icon_button_size"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:focusable="true"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivHeaderActionIcon"
                android:layout_width="@dimen/header_icon_size"
                android:layout_height="@dimen/header_icon_size"
                android:layout_gravity="center"
                android:contentDescription="Hành động chính"
                android:src="@drawable/ic_refresh_gray"
                app:tint="@color/app_text_primary" />

            <!-- Red Realtime Alert Badge with White Border -->
            <TextView
                android:id="@+id/tvHeaderActionBadge"
                android:layout_width="wrap_content"
                android:layout_height="18dp"
                android:minWidth="18dp"
                android:layout_gravity="top|end"
                android:layout_marginTop="-2dp"
                android:layout_marginEnd="-2dp"
                android:background="@drawable/bg_badge_count_red"
                android:gravity="center"
                android:includeFontPadding="false"
                android:paddingHorizontal="4dp"
                android:text="3"
                android:textColor="@color/white"
                android:textSize="9.5sp"
                android:textStyle="bold"
                android:visibility="gone" />
        </FrameLayout>
    </LinearLayout>

</LinearLayout>
